---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: springboot                  # Ingress name
  labels:
    app: springboot
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "springboot.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: springboot               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
